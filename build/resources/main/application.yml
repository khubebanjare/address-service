server:
  port: 9081

spring:
  application:
    name: address-service
  config:
    import: "optional:configserver:http://localhost:8888"

info:
  app:
    name: ${spring.application.name}
    description: "Address Service for managing address data"
    version: "1.0.0"